import React from 'react'; // we need this to make JSX compile

// type Loading = {
//     title?: string,
//     paragraph?: string
// }

export const Loading = () => {
    return (
        <h1>loading...</h1>
    )

}

